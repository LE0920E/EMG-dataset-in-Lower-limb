

# 数据说明与预处理报告

## 1. 数据来源与采集方式

本研究使用的数据来自 **22 名男性受试者**，其中包括：
- **11 名膝关节异常患者**（Abnormal，标记为 `A`）
- **11 名健康受试者**（Normal，标记为 `N`）

每位受试者在三种不同动作状态下进行数据采集：
- **步行（march/gait）**
- **坐姿（sitting）**
- **站立（standing）**

### 1.1 采集设备与参数
- 使用 **Biometrics DataLOG MWX8** 表面肌电（sEMG）采集系统。
- 采样频率：**1000 Hz**（即每秒采集 1000 个数据点）。
- 分辨率：14 位。
- 信号类型：原始未滤波（raw, no filters）。
- 数据采集时间：2012年7月、2013年3月、2013年7月。

### 1.2 电极与传感器布置
在优势侧下肢的四个关键肌肉上贴附表面电极，同步采集膝关节屈伸角度：
| 肌肉/信号 | 对应通道 | 英文名 | 电极位置 |
|----------|---------|--------|----------|
| 股直肌 | Ch1 | Rectus Femoris (RF) | 大腿前侧 |
| 股二头肌 | Ch2 | Biceps Femoris (BF) | 大腿后侧 |
| 股内侧肌 | Ch3 | Vastus Medialis (VM) | 大腿内侧 |
| 半腱肌 | Ch4 | Semitendinosus (ST) | 大腿后内侧 |
| 膝关节屈伸角度 | Ch5 | Knee Flexion (FX) | 膝关节处 |

所有信号均通过蓝牙实时传输并存储于设备内部内存，同时传输至计算机软件。

### 1.3 数据采集协议
- 每个受试者进行3-5次重复的每个动作练习
- 数据采集地点：Batallón de sanidad (BASAN) Universidad Militar Nueva Granada (2012年7月)、TecnoParque SENA nodo Manizales (2013年3月-7月)

---

## 2. 数据文件结构

### 2.1 原始数据文件组织
原始数据按受试者状态分类，存储在四个主文件夹中：

```
SEMG_DB1/
├── A_LOG/           # 异常组原始数据（.log格式）
│   ├── 1Amar.log
│   ├── 1Asen.log
│   ├── 1Asitting.log
│   └── ...（共 33 个文件）
├── A_TXT/           # 异常组文本数据（.txt格式）
│   ├── 1Amar.txt
│   ├── 1Asen.txt
│   ├── 1Asitting.txt
│   └── ...（共 33 个文件）
├── N_LOG/           # 正常组原始数据（.log格式）
│   ├── 1Nmar.log
│   ├── 1Nsen.log
│   ├── 1Nsitting.log
│   └── ...（共 33 个文件）
└── N_TXT/           # 正常组文本数据（.txt格式）
    ├── 1Nmar.txt
    ├── 1Nsen.txt
    ├── 1Nsitting.txt
    └── ...（共 33 个文件）
```

### 2.2 预处理后数据文件组织
预处理后的数据按受试者状态分类，存储在两个主文件夹中：

```
gaits_data/
├── Abnormal/        # 异常组（11人）
│   ├── 1Agait - Sheet1.csv
│   ├── 1Asitting - Sheet1.csv
│   ├── 1Astanding - Sheet1.csv
│   ├── 2Agait - Sheet1.csv
│   └── ...（共 33 个文件）
└── Normal/          # 正常组（11人）
    ├── 1Ngait - Sheet1.csv
    ├── 1Nsitting - Sheet1.csv
    ├── 1Nstanding - Sheet1.csv
    └── ...（共 33 个文件）
```

> 总计：22 人 × 3 动作 = **66 个 CSV 文件**

### 2.3 文件命名规则
原始文件名格式为：
```
{编号}{状态}{动作}.{扩展名}
```
预处理后文件名格式为：
```
{编号}{状态}{动作} - Sheet1.csv
```

动作代码说明：
- `mar` → 步行（march/gait）
- `sen` → 坐姿（sitting）
- `pie` → 站立（standing）

例如：
- `1Amar.txt` → 第 1 号异常受试者，步行状态（原始文本格式）
- `10Nsitting - Sheet1.csv` → 第 10 号正常受试者，坐姿状态（预处理后CSV格式）

---

## 3. 数据属性与格式

### 3.1 原始数据格式

原始数据文件包含5个通道的数据：

| 通道 | 肌肉/信号 | 英文名 | 单位 | 采样频率 | 数据点数 |
|------|-----------|--------|------|----------|----------|
| Ch1 | 股直肌 | Rectus Femoris (RF) | mV | 1000 Hz | 15300 |
| Ch2 | 股二头肌 | Biceps Femoris (BF) | mV | 1000 Hz | 15300 |
| Ch3 | 股内侧肌 | Vastus Medialis (VM) | mV | 1000 Hz | 15300 |
| Ch4 | 半腱肌 | Semitendinosus (ST) | mV | 1000 Hz | 15300 |
| Ch5 | 膝关节屈伸角度 | Knee Flexion (FX) | 度（°） | 50 Hz → 1000 Hz | 765（插值后） |

> 注：膝关节角度数据原始采样频率为50 Hz，通过插值方法扩展到1000 Hz以与其他通道同步。

### 3.2 预处理后数据格式（CSV 文件结构）

预处理后的CSV文件包含5列数据：

| 列名 | 物理意义 | 单位 | 数据类型 | 对应原始通道 |
|------|--------|------|----------|-------------|
| `Recto Femoral` | 股直肌肌电信号 | mV | 浮点数 | Ch1 |
| `Biceps Femoral` | 股二头肌肌电信号 | mV | 浮点数 | Ch2 |
| `Vasto Medial` | 股内侧肌肌电信号 | mV | 浮点数 | Ch3 |
| `EMG Semitendinoso` | 半腱肌肌电信号 | mV | 浮点数 | Ch4 |
| `Flexo-Extension` | 膝关节屈伸角度 | 度（°） | 浮点数 | Ch5 |

> 每行代表一个时间点的同步测量值。

### 3.3 时间间隔（Time Step）

- **采样频率**：1000 Hz
- **时间步长 Δt**：  
  $$
  \Delta t = \frac{1}{1000} = 0.001\ \text{秒} = 1\ \text{毫秒}
  $$
- 每两个连续数据点之间的时间间隔为 **1 ms**。
- 每个数据文件包含约15.3秒的数据（15300个数据点）

例如：一个包含 1000 行的数据段 → 持续时间为 1 秒。

---

## 4. 数据预处理流程（Python 程序说明）

在输入模型前，数据经过以下处理步骤：

### 4.1 预处理模块功能概述

使用自定义 `EMGDataset` 类封装数据加载与预处理逻辑，主要功能包括：
- 自动遍历文件夹加载所有 `.csv` 文件
- 统一列名格式（忽略大小写和空格）
- 信号滤波（巴特沃斯带通滤波）
- 标准化（Z-score标准化）
- 标签解析（从文件名提取动作和状态标签）
- 窗口切片（支持变长序列，不进行零填充）
- 动态批处理支持

---

### 4.2 具体处理步骤

| 步骤 | 方法 | 参数说明 | 实现细节 |
|------|------|----------|----------|
| **1. 读取 CSV 文件** | `pandas.read_csv()` | 加载预处理后的CSV数据 | 直接读取gaits_data文件夹中的CSV文件 |
| **2. 列名标准化** | `str.strip().str.lower()` | 统一为小写，去除空格 | 确保列名兼容性，支持多种命名格式 |
| **3. 通道对齐** | 动态匹配列名 | 支持 `flexo-extension`, `Flexo Extension` 等变体 | 自动识别膝关节角度列 |
| **4. 带通滤波** | 巴特沃斯滤波器（`scipy.signal.filtfilt`） | 截止频率：20–450 Hz，阶数：4 | 去除低频噪声和高频干扰 |
| **5. 标准化（Z-score）** | `sklearn.preprocessing.StandardScaler` | 按所有训练数据拟合均值与标准差 | 基于训练集数据拟合，应用于所有数据 |
| **6. 标签提取** | 从文件名解析 | 动作标签（gait/sitting/standing），状态标签（N/A） | 支持动作代码：mar/gait, sen/sitting, pie/standing |
| **7. 窗口切片** | 随机滑动窗口 | 默认长度 `seq_length=1000`（即 1 秒） | 支持变长序列，不足时不进行零填充 |
| **8. 动态批处理** | 自定义 `dynamic_collate_fn` | 支持不同长度序列的填充与长度记录 | 返回实际序列长度用于RNN处理 |

### 4.3 滤波参数详细说明

- **滤波器类型**：巴特沃斯带通滤波器（Butterworth Bandpass Filter）
- **低通截止频率**：20 Hz（去除低频噪声和运动伪影）
- **高通截止频率**：450 Hz（去除高频干扰和电源噪声）
- **采样频率**：1000 Hz
- **滤波器阶数**：4阶
- **滤波方法**：零相位滤波（`filtfilt`），避免相位失真

### 4.4 标准化处理

- **方法**：Z-score标准化（StandardScaler）
- **计算方式**：$x_{norm} = \frac{x - \mu}{\sigma}$
- **拟合数据**：使用所有训练集数据计算均值和标准差
- **应用范围**：训练集和测试集使用相同的标准化参数

### 4.5 窗口切片策略

- **默认窗口长度**：1000个时间点（对应1秒数据）
- **变长处理**：
  - 当序列长度 ≥ 1000时：随机选择起始位置截取1000个点
  - 当序列长度 < 1000时：使用所有可用数据，不进行零填充
- **随机性**：训练时随机选择窗口位置，增加数据多样性

---

## 5. 模型输入数据形状与结构

经过预处理后，每个批次的数据以张量形式送入模型。

### 5.1 单个样本（窗口）形状

| 维度 | 大小 | 说明 |
|------|------|------|
| 时间步数（T） | 1000（默认） | 对应 1 秒时长（1000 × 1ms），支持变长序列 |
| 特征数（C） | 5 | 4 个肌电通道 + 1 个角度通道 |
| 形状 | `(T, 5)` | 二维张量，T ∈ [min_len, 1000] |

> 示例：一个 `gait` 动作片段被切分为多个 `(T, 5)` 的窗口，其中T为实际序列长度。

### 5.2 数据通道详细信息

| 通道索引 | 信号类型 | 肌肉名称 | 英文名 | 单位 |
|----------|----------|----------|--------|------|
| 0 | 肌电信号 | 股直肌 | Rectus Femoris (RF) | mV |
| 1 | 肌电信号 | 股二头肌 | Biceps Femoris (BF) | mV |
| 2 | 肌电信号 | 股内侧肌 | Vastus Medialis (VM) | mV |
| 3 | 肌电信号 | 半腱肌 | Semitendinosus (ST) | mV |
| 4 | 角度信号 | 膝关节屈伸 | Knee Flexion/Extension | 度（°） |

---

### 5.3 批次数据形状（DataLoader 输出）

使用 `dynamic_collate_fn` 实现动态批处理，一个批次包含：

| 输出项 | 张量形状 | 说明 | 数据类型 |
|--------|---------|------|----------|
| `emg_seq` | `[B, L_max, 5]` | 填充后的序列，B=批次大小，L_max=该批次中最长序列长度 | `torch.float32` |
| `seq_lengths` | `[B]` | 每个序列的实际长度（用于 RNN 处理变长序列） | `torch.int64` |
| `action_label` | `[B]` | 动作类别标签（0=gait, 1=sitting, 2=standing） | `torch.int64` |
| `status_label` | `[B]` | 状态标签（0=Normal, 1=Abnormal） | `torch.int64` |

> 当前设置：`BATCH_SIZE = 32`

### 5.4 标签定义

**动作标签（3分类）：**
- `0` → 步行（gait/march）
- `1` → 坐姿（sitting）
- `2` → 站立（standing）

**状态标签（2分类）：**
- `0` → 正常（Normal）
- `1` → 异常（Abnormal）

### 5.5 动态批处理机制

- **填充策略**：使用零填充（zero-padding）将批次内所有序列对齐到最长序列长度
- **长度记录**：`seq_lengths` 张量记录每个序列的实际有效长度
- **模型适配**：支持RNN、LSTM、Transformer等处理变长序列的模型
- **内存优化**：避免不必要的零填充，提高计算效率

---

## 6. 数据集划分

### 6.1 原始数据文件划分

| 数据集 | 文件数量 | 说明 |
|--------|---------|------|
| 总文件数 | 66 | 22名受试者 × 3种动作状态 |
| 训练集 | ~53 | 80% 随机划分（固定随机种子 42） |
| 测试集 | ~13 | 20% 随机划分 |

### 6.2 训练时窗口划分

由于每个原始CSV文件包含约15.3秒的数据（15300个时间点），在训练时会进一步切分为多个窗口：

| 项目 | 数值 | 说明 |
|------|------|------|
| 原始数据时长 | ~15.3秒 | 每个CSV文件 |
| 窗口长度 | 1秒 | 1000个时间点 |
| 每个文件窗口数 | ~15个 | 通过滑动窗口生成 |
| 总训练窗口数 | ~795个 | 53个文件 × 15个窗口 |
| 总测试窗口数 | ~195个 | 13个文件 × 15个窗口 |

### 6.3 划分策略

- **随机划分**：使用PyTorch的`random_split`函数
- **固定随机种子**：`torch.Generator().manual_seed(42)`确保可重复性
- **文件级别划分**：在文件级别进行划分，避免同一受试者的不同动作被分到不同集合
- **数据泄露防护**：确保训练集和测试集来自不同的受试者

---

## 7. 总结表：关键参数一览

### 7.1 原始数据参数

| 项目 | 内容 |
|------|------|
| **总人数** | 22（11 正常 + 11 异常） |
| **动作类型** | 步行（march/gait）、坐姿（sitting）、站立（standing） |
| **采集设备** | Biometrics DataLOG MWX8 |
| **采样频率** | 1000 Hz（肌电信号），50 Hz（角度信号，插值到1000 Hz） |
| **时间步长** | 1 ms |
| **信号通道** | 4 个 sEMG + 1 个角度计 |
| **原始数据点数** | 15300个/通道（约15.3秒） |
| **数据格式** | .log（原始格式），.txt（文本格式） |

### 7.2 预处理参数

| 项目 | 内容 |
|------|------|
| **预处理后格式** | CSV（已滤波和标准化） |
| **滤波方法** | 20–450 Hz 巴特沃斯带通滤波（4阶） |
| **标准化方法** | StandardScaler（Z-score） |
| **窗口长度** | 1000 时间点（1 秒），支持变长序列 |
| **零填充策略** | 不进行零填充，使用实际可用数据 |
| **批次大小** | 32 |

### 7.3 模型输入参数

| 项目 | 内容 |
|------|------|
| **输入形状** | `(T, 5)`，T ∈ [min_len, 1000] |
| **特征维度** | 5（4个肌电通道 + 1个角度通道） |
| **批次处理** | 动态填充 + 实际长度记录 |
| **标签类型** | 多任务：动作分类（3类）+ 状态分类（2类） |
| **数据划分** | 80% 训练集，20% 测试集 |

### 7.4 技术特点

- **多模态融合**：结合肌电信号和运动学数据
- **变长序列支持**：动态批处理机制
- **零相位滤波**：避免信号失真
- **多任务学习**：同时识别动作和状态
- **数据增强**：随机窗口选择增加多样性

---

