

# 数据说明与预处理报告

## 1. 数据来源与采集方式

本研究使用的数据来自 **22 名男性受试者**，其中包括：
- **11 名膝关节异常患者**（Abnormal，标记为 `A`）
- **11 名健康受试者**（Normal，标记为 `N`）

每位受试者在三种不同动作状态下进行数据采集：
- **步行（gait）**
- **坐姿（sitting）**
- **站立（standing）**

### 1.1 采集设备
- 使用 **Biometrics MWX8 Datalog** 表面肌电（sEMG）采集系统。
- 采样频率：**1000 Hz**（即每秒采集 1000 个数据点）。
- 分辨率：14 位。
- 信号类型：原始未滤波（raw, no filters）。

### 1.2 电极与传感器布置
在优势侧下肢的四个关键肌肉上贴附表面电极，同步采集关节角度：
| 肌肉/信号 | 对应通道 | 英文名 |
|----------|---------|--------|
| 股直肌 | Recto Femoral | Rectus Femoris (RF) |
| 股二头肌 | Biceps Femoral | Biceps Femoris (BF) |
| 股内侧肌 | Vasto Medial | Vastus Medialis (VM) |
| 半腱肌 | EMG Semitendinoso | Semitendinosus (ST) |
| 膝关节屈伸角度 | Flexo-Extension | Goniometer 输出（单位：°） |

所有信号均通过蓝牙实时传输并存储于 microSD 卡中。

---

## 2. 数据文件结构

### 2.1 文件组织方式
数据按受试者状态分类，存储在两个主文件夹中：

```
gaits_data/
├── Abnormal/        # 异常组（11人）
│   ├── 1Agait.csv
│   ├── 1Asitting.csv
│   ├── 1Astanding.csv
│   ├── 2Agait.csv
│   └── ...（共 33 个文件）
└── Normal/          # 正常组（11人）
    ├── 1Ngait.csv
    ├── 1Nsitting.csv
    ├── 1Nstanding.csv
    └── ...（共 33 个文件）
```

> 总计：22 人 × 3 动作 = **66 个 CSV 文件**

### 2.2 文件命名规则
文件名格式为：
```
{编号}{状态}{动作} - Sheet1.csv
```
例如：
- `1Agait - Sheet1.csv` → 第 1 号异常受试者，步行状态
- `10Nsitting - Sheet1.csv` → 第 10 号正常受试者，坐姿状态

> 注：`1Ngait - 1Nmar.csv` 是命名异常文件，但内容仍为 gait 数据。

---

## 3. 数据属性与格式

### 3.1 数据列信息（CSV 文件结构）

| 列名 | 物理意义 | 单位 | 数据类型 |
|------|--------|------|----------|
| `Recto Femoral` | 股直肌肌电信号 | mV | 浮点数 |
| `Biceps Femoral` | 股二头肌肌电信号 | mV | 浮点数 |
| `Vasto Medial` | 股内侧肌肌电信号 | mV | 浮点数 |
| `EMG Semitendinoso` | 半腱肌肌电信号 | mV | 浮点数 |
| `Flexo-Extension` | 膝关节屈伸角度 | 度（°） | 浮点数 |

> 每行代表一个时间点的同步测量值。

### 3.2 时间间隔（Time Step）

- **采样频率**：1000 Hz
- **时间步长 Δt**：  
  $$
  \Delta t = \frac{1}{1000} = 0.001\ \text{秒} = 1\ \text{毫秒}
  $$
- 每两个连续数据点之间的时间间隔为 **1 ms**。

例如：一个包含 1000 行的数据段 → 持续时间为 1 秒。

---

## 4. 数据预处理流程（Python 程序说明）

在输入模型前，数据经过以下处理步骤：

### 4.1 预处理模块功能概述

使用自定义 `EMGDataset` 类封装数据加载与预处理逻辑，主要功能包括：
- 自动遍历文件夹加载所有 `.csv` 文件
- 统一列名格式（忽略大小写和空格）
- 信号滤波
- 标准化
- 标签解析
- 窗口切片
- 动态批处理支持

---

### 4.2 具体处理步骤

| 步骤 | 方法 | 参数说明 |
|------|------|----------|
| **1. 读取 CSV 文件** | `pandas.read_csv()` | 加载原始数据 |
| **2. 列名标准化** | `str.strip().str.lower()` | 统一为小写，去除空格 |
| **3. 通道对齐** | 动态匹配列名 | 支持 `flexo-extension`, `Flexo Extension` 等变体 |
| **4. 带通滤波** | 巴特沃斯滤波器（`scipy.signal.filtfilt`） | 截止频率：20–450 Hz，阶数：4 |
| **5. 标准化（Z-score）** | `sklearn.preprocessing.StandardScaler` | 按所有训练数据拟合均值与标准差 |
| **6. 标签提取** | 从文件名解析 | 动作标签（gait/sitting/standing），状态标签（N/A） |
| **7. 窗口切片** | 随机滑动窗口 | 固定长度 `seq_length=1000`（即 1 秒） |
| **8. 动态批处理** | 自定义 `collate_fn` | 支持不同长度序列的填充与长度记录 |

---

## 5. 模型输入数据形状与结构

经过预处理后，每个批次的数据以张量形式送入模型。

### 5.1 单个样本（窗口）形状

| 维度 | 大小 | 说明 |
|------|------|------|
| 时间步数（T） | 1000 | 对应 1 秒时长（1000 × 1ms） |
| 特征数（C） | 5 | 4 个肌电通道 + 1 个角度通道 |
| 形状 | `(1000, 5)` | 二维张量 |

> 示例：一个 `gait` 动作片段被切分为多个 `(1000, 5)` 的窗口。

---

### 5.2 批次数据形状（DataLoader 输出）

使用 `dynamic_collate_fn` 实现动态批处理，一个批次包含：

| 输出项 | 张量形状 | 说明 |
|--------|---------|------|
| `emg_seq` | `[B, L_max, 5]` | 填充后的序列，B=批次大小，L_max=该批次中最长序列长度 |
| `seq_lengths` | `[B]` | 每个序列的实际长度（用于 RNN 处理变长序列） |
| `action_label` | `[B]` | 动作类别标签（0=gait, 1=sitting, 2=standing） |
| `status_label` | `[B]` | 状态标签（0=Normal, 1=Abnormal） |

> 当前设置：`BATCH_SIZE = 32`

---

## 6. 数据集划分

| 数据集 | 样本数 | 说明 |
|--------|-------|------|
| 总样本数 | 66 | 每个 CSV 文件作为一个原始样本 |
| 训练集 | ~53 | 80% 随机划分（固定随机种子 42） |
| 测试集 | ~13 | 20% 随机划分 |

> 注意：每个原始文件（如 `1Agait.csv`）可能包含数千个时间点，在 `__getitem__` 中会进一步切分为多个 `(1000, 5)` 的窗口用于训练。

---

## 7. 总结表：关键参数一览

| 项目 | 内容 |
|------|------|
| **总人数** | 22（11 正常 + 11 异常） |
| **动作类型** | 步行、坐姿、站立（共 3 类） |
| **采集设备** | Biometrics MWX8 Datalog |
| **采样频率** | 1000 Hz |
| **时间步长** | 1 ms |
| **信号通道** | 4 个 sEMG + 1 个角度计 |
| **原始数据格式** | CSV（未滤波） |
| **预处理滤波** | 20–450 Hz 巴特沃斯带通滤波 |
| **标准化方法** | StandardScaler（Z-score） |
| **窗口长度** | 1000 时间点（1 秒） |
| **输入形状** | `(T, 5)`，T ∈ [min_len, 1000] |
| **批次处理** | 动态填充 + 实际长度记录 |
| **标签类型** | 多任务：动作分类 + 状态分类 |

---

